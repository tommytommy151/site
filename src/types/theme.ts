export interface ThemeSettings {
  primaryColor: string;
  accentColor: string;
  radius: number;
}

export interface ThemePreset {
  id: string;
  name: string;
  primaryColor: string;
  accentColor: string;
}
